HTML is like the basic framework for your house
CSS is how we make the house look pretty
JavaScript is what allows the house to be interactive: plumbing, electricity

tags = <h1>, <p>, <section>, <meta> etc

elements = tags, attributes, and content
  - attributes define the specifics about a tag
  - content is generally what the user sees BUT can see other elements
